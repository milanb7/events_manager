import { RunningFilterPipe } from './running-filter.pipe';

describe('RunningFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RunningFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
