import { FutureFilterPipe } from './future-filter.pipe';

describe('FutureFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FutureFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
