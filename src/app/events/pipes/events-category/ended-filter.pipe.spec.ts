import { EndedFilterPipe } from './ended-filter.pipe';

describe('EndedFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EndedFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
